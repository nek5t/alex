<!-- Details.stories.mdx -->

import { Meta, Story, Canvas } from '@storybook/addon-docs';

import Details from '.';

export const staticProps = {
	details: <p>Cool Details</p>,
};

export const Template = (args) => (
	<Details {...staticProps} summary={<h3>{args.summary}</h3>} />
);

<Meta title="Blocks/Details" component={Details} />

# Details Disclosure

An expandable details element with summary heading.

<Canvas>
	<Story
		name="Default"
		argTypes={{
			details: {
				control: false,
			},
			innerBlocks: {
				control: false,
			},
			props: {
				control: false,
			},
		}}
		args={{
			summary: 'Cool Summary',
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>
