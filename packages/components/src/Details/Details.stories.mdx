<!-- Details.stories.mdx -->

import { Meta, Story, Canvas } from '@storybook/addon-docs';

import Details from '.';

export const Template = (args) => <Details {...args} />;

<Meta title="Blocks/Alex/Details" component={Details} />

# Details Disclosure

An expandable details element with summary heading.

<Canvas>
	<Story
		name="Default"
		argTypes={{
			details: {
				control: false,
			},
			innerBlocks: {
				control: false,
			},
			props: {
				control: false,
			},
		}}
		args={{
			summary: 'Cool Summary',
			details: <p>Cool Details</p>,
			asDiv: false,
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>
